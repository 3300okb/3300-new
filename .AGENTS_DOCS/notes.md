# Notes

- Do not edit generated JSON in `public/` by hand; run `npm run index:generate`.
- Vite alias `@` points to `src` (see `vite.config.ts`).
- Chunking rules in `vite.config.ts` group articles by category; keep categories consistent.
