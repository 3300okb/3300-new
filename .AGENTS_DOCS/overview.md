# Overview

- Vue 3 + Vite + TypeScript + Tailwind CSS project.
- Main UI lives in `src/App.vue`; app bootstraps in `src/main.ts`.
- Articles are Vue SFCs under `src/components/article/<category>/<name>.vue`.
- Article list/search indexes are generated into `public/` and fetched at runtime.
